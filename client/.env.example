# Same as entriks-hr: leave empty locally (Vite proxies /api).
# On Vercel you can set this, or omit it — production defaults to https://vantaro.onrender.com
# VITE_API_URL=https://vantaro.onrender.com
VITE_API_URL=
